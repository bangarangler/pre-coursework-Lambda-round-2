// getThird takes in an array it reutrns the third item in the array if no third return null ex. getThird(['doc','biff','morty'])
// returns morty
//function getThird(arr) {
		//if (arr.length < 3) {
		//return null;
//} else {
		//return arr[2];
//}
//}


//string lengths takes in four strings it returns array containing the length of each string
//example stringLengths('mushroom','onion','','garlic') returns [8,5,0,6]
//function stringLength(str1,str2,str3,str4) {
		//return ([str1.length,str2.length,str3.length,str4.length]);
//}

//remove first takes in an array and returns the same array but with the first item removed
//function removeFirst(arr) {
	 //arr.shift();
		//return arr;
//}


//swap takes in an array and two indices and returns the array with the items at i and j swapped
//ex swap(['unicycle','scooter','skateboard',canoe'],0,2) returns ['skateboard','scooter','unicycle','canor']
//function swap(arr,x,z) {
		//var flip1 = arr[x];
		//var flip2 = arr[z];
		//arr[x] = flip2;
		//arr[z] = flip1;
		//return arr;
//}


//getValue takes in an object and a key name if the key exists in the object, return the value if not return null
//example getValue({first: 'Gordon', Last: 'Peppermill', 'last') returns 'Peppermill'
//function getValue(object, key) {
		//if (key in object) {
				//return object[key];
		//} else return null;
//}
//generateCSS takes in three strings containing css values. it returns an object with the css key value pairs
//ex. generateCSS('100px','70px','blue' would return {height: '100px', width: '70px', color: 'blue',};
//function generateCSS(str1,str2,str3) {
		//var css = {height: str1,
				//width: str2,
				//color: str3,
		//};
				//return css;
		//}


// getValues takes in an objects and return an array containing the values from the object
// example getValues({name: 'Ally', age: 27, job: 'hypnotist'}) would return ['ally', 27, 'hypnotist']
//function getValues(object) {
		//const valuesOfArray = [];
		//for (let key in object) {
				//valuesOfArray.push(object[key]);
				//return valuesOfArray;
		//}
//}

//function getValues(obj) {
		//const valuesOfArray = [];
		//for (let key in obj) {
				//valuesOfArray.push(obj[key]);
		//} return valuesOfArray;
//}


// wrapArround
// count
