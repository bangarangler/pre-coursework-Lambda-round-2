// function to add one
//function addOne(x) {
		//plusOne = x + 1;
		//return plusOne;
//}

// function to divide x y
//function divide(x,y) {
		//return x / y;
//}

// function getBiggest x y return the largest
//function getBiggest(x,y) {
		//if (x > y) {
				//return x;
		//} else if (y >x) {
				//return y;
		//}
//}

//getLength str takes a string and returns the length of string ex. 'tazerface' returns 9
//function getLength(str) {
		//return str.length;
//}

// orderPizza takes in boolean if true return string 'cheese pizza' if false return 'pepperoni pizza'
//function orderPizza(vegetarian) {
		//if (vegetarian === true) {
				//return ("cheese pizza");
		//} else if (vegetarian == false);
		//return ('pepperoni pizza');
//}
//const vegetarian = true;
//const pizzalover = false;

// countdown takes in a number greater than 0 it logs every number starting from the input down to zero. there is no return value
//function countDown(x) {
		//if (x < 0) {
				//for (let i = x; i <= 0; i++) {
						//console.log(i);
				//}
		//}
		 //if (x > 0) {
				//for (let i = x; i >= 0; i--) {
						//console.log(i);
				//}
		 //}
//}






//repeat logs a phrase x number of times, no return value, repeat("not your", 4)
//function repeat(phrase, x) {
		//for (let i = 0; i < x; i++) {
				//console.log(phrase);
		//}
//}


// get HTMLTAG takes in a tag name and returns the opening and closing tag
//function getHTMLTag(tag) {
		//return "<" + tag + ">" + "</" + tag + ">";
//}

//get header tag takes in header text and header size and returns opening and closing tag with the text inside ex getHeaderTag('welcome', 2) would return '<h2>Welcome</h2>
//function getHeaderTag(tag,x) {
		//return '<' + tag + x + '>' + '</' + tag + x + '>';
//}


